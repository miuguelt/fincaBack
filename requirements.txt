# Core Flask dependencies
blinker==1.8.2
click==8.1.7
colorama==0.4.6
Flask==3.0.3
Flask-Cors==4.0.0
Flask-JWT-Extended==4.6.0
Flask-SQLAlchemy==3.1.1
flask-restx==1.3.0
Werkzeug==3.0.3

# Database dependencies
SQLAlchemy==2.0.32
PyMySQL==1.1.1
# mysqlclient==2.2.4  # Opcional (requiere Build Tools y no hay wheel para Python 3.13 aún)
# mysql-connector-python==9.0.0  # Alternativa pura Python (driver: mysql+mysqlconnector)
# greenlet==3.0.3  # Opcional (puede fallar compilación en Python 3.13 en Windows)

# Security dependencies
cryptography==42.0.8
PyJWT==2.9.0

# Utility dependencies
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5
packaging==24.1
typing_extensions==4.12.2
python-dotenv==1.0.1

# Production server
gunicorn==22.0.0

# Performance monitoring dependencies
psutil==5.9.8

# Development and testing dependencies (optional)
# pytest==8.2.2
# pytest-flask==1.3.0
# pytest-cov==5.0.0
# black==24.4.2
# flake8==7.1.0

# Optional: Redis for advanced caching (uncomment if needed)
# redis==5.0.7
# Flask-Caching==2.3.0

# Optional: Celery for background tasks (uncomment if needed)
# celery==5.4.0

# Optional: Elasticsearch for advanced search (uncomment if needed)
# elasticsearch==8.14.0


